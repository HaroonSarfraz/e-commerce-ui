import { AUTH_URL } from '../constants';

export default {
  authUrl: AUTH_URL,
  userAttributes: {
    name: 'name',
    email: 'email',
    id: 'id'
  },

  userRegistrationAttributes: {
    name: 'name',
    id: 'id'
  }
}
